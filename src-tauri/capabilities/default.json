{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "MT 管理器默认权限",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "store:default"
  ]
}
